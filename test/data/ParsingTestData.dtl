import var createCounter(final var name);
import var ser_Voice;
var TRUE = true;

var lookupForErrorInfo(var errCauseType, var errCauseCode) {
    var v = TRUE;
    createCounter("test");
}

var user(final var param1) {
    var v = lookupForErrorInfo("1", "2");

    for(var i=0; i<10; i++) {
        v = i;
    }

    for(var i : param1) {
        v = i;
    }
}